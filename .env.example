# Personal Mode requires no environment variables or API keys.
# Team Mode uses server-only configuration. Never put passwords or employee data in client code.
PORT=4173
KAD_TEAM_PORT=3000
KAD_TEAM_HOST=127.0.0.1
KAD_DATA_DIR=backend/data
KAD_COOKIE_SECURE=false
